@model Pokedex.Models.PokemonTeamViewModel
@{
    ViewBag.Title = "Delete Team";
}

<h2>Delete Team "@Model.PokemonTeamName"</h2>

<div class="form-horizontal">

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        
        @Html.HiddenFor(x => x.Id)

        <div class="pokemonList shadowed">
            @if (@Model.FirstPokemon != null)
            {
                <img loading="lazy" class="lightshadowed pokemonTeamPokemon" src="@Url.Content(string.Concat(@Model.AppConfig.WebUrl, @Model.AppConfig.OfficialPokemonImageUrl, @Model.FirstPokemon.PokemonId, ".png"))" />
            }

            @if (@Model.SecondPokemon != null)
            {
                <img loading="lazy" class="lightshadowed pokemonTeamPokemon" src="@Url.Content(string.Concat(@Model.AppConfig.WebUrl, @Model.AppConfig.OfficialPokemonImageUrl, @Model.SecondPokemon.PokemonId, ".png"))" />
            }
            
            @if (@Model.ThirdPokemon != null)
            {
                <img loading="lazy" class="lightshadowed pokemonTeamPokemon" src="@Url.Content(string.Concat(@Model.AppConfig.WebUrl, @Model.AppConfig.OfficialPokemonImageUrl, @Model.ThirdPokemon.PokemonId, ".png"))" />
            }
            
            @if (@Model.FourthPokemon != null)
            {
                <img loading="lazy" class="lightshadowed pokemonTeamPokemon" src="@Url.Content(string.Concat(@Model.AppConfig.WebUrl, @Model.AppConfig.OfficialPokemonImageUrl, @Model.FourthPokemon.PokemonId, ".png"))" />
            }
            
            @if (@Model.FifthPokemon != null)
            {
                <img loading="lazy" class="lightshadowed pokemonTeamPokemon" src="@Url.Content(string.Concat(@Model.AppConfig.WebUrl, @Model.AppConfig.OfficialPokemonImageUrl, @Model.FifthPokemon.PokemonId, ".png"))" />
            }
            
            @if (@Model.SixthPokemon != null)
            {
                <img loading="lazy" class="lightshadowed pokemonTeamPokemon" src="@Url.Content(string.Concat(@Model.AppConfig.WebUrl, @Model.AppConfig.OfficialPokemonImageUrl, @Model.SixthPokemon.PokemonId, ".png"))" />
            }
        </div>

        <br>

        <p>
            <button type="submit" class="btn btn-danger" role="button">Delete</button>
        </p>
    }

</div>