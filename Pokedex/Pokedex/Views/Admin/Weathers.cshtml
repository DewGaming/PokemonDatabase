@model IEnumerable<Pokedex.DataAccess.Models.Weather>;
@{
    Layout = "_Layout";
}

<div class="timeTable">
    @if (this.User.IsInRole("Owner"))
    {
        <p>
            <a class="btn btn-primary" href="@this.Url.Action("Weather", "Add")" role="button">Add Weather</a>
        </p>
    }
    <table class="table">
        <tr>
            <th scope="col">Weather</th>
            <th scope="col">Actions</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Name
                </td>
                <td>
                    <a href="@(this.Url.Action("Weather", "Edit", new { id = item.Id }))">Edit</a>
                    @if (this.User.IsInRole("Owner"))
                    {
                        @('|')
                        <a href="@(this.Url.Action("Weather", "Delete", new { id = item.Id }))">Delete</a>
                    }
                </td>
            </tr>
        }
    </table>
</div>