@model IEnumerable<Pokedex.DataAccess.Models.ReviewedPokemon>;
@{
    Layout = "_Layout";
}

@if(Model.Count() > 0)
{
    <a class="btn btn-primary" role="button" href="@(Url.Action("CompleteReviewedPokemon", "Owner"))">Complete Reviewed Pokemon</a>
}

<div class="reviewedPokemonTable">
    <table class="table">
        <tr>
            <th scope="col">Pokemon Id</th>
            <th scope="col">Actions</th>
        </tr>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.PokemonId</td>
                <td><a href="@(Url.Action("ReviewedPokemon", "Delete", new { id = item.Id }))">Delete</a></td>
            </tr>
        }
    </table>
</div>